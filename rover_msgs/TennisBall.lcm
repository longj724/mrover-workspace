package rover_msgs;

struct TennisBall {
	boolean found;
	boolean frontBack;
	double bearing; // from straight ahead
	double distance;
	double tagBearing; // angle the tag is facing relative to the rover
}

pair<TennisBall, TennisBall>